seqfile = 
outfile = 
treefile = 

noisy = 4 * how much rubbish on the screen (medium)
verbose = 1 * how detailed is the output (detailed)

seqtype = 1 * data type (codons)
ndata = 1 * number of dataset/genes per input file (only 1)
icode = 0 * genetic code (universal)
cleandata = 0 * remove sites with ambiguity data? (no)

model = 2 * models for varying omega across branches (different omega values for branches)
NSsites = 2 * models for varying omega across sites (different omega values for sites)
* this combination specifies the branch-site model

CodonFreq = 7 * codon frequencies ()
estFreq = 0 * use observed frequencies or estimate by ML (observed)
clock = 0 * clock model (no clock)
fix_omega = 1 * estimate or fix omega (fix omega2)
omega = 1 * fixed omega